name: Keep Alive Render
on:
  schedule:
    - cron: '*/14 * * * *' # Runs every 14 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl website
        run: curl https://local-library-mdn.onrender.com
